#!/usr/bin/env node
'use strict';

const fs=require('fs'),path=require('path'),vm=require('vm');
const {installServiceRegistry}=require('./test-service-registry');
const ROOT=path.resolve(__dirname,'..');
const source=fs.readFileSync(path.join(ROOT,'app/src/main/assets/proceed-electrical.js'),'utf8');
const inputSource=fs.readFileSync(path.join(ROOT,'app/src/main/assets/dsky-input-runtime.js'),'utf8');
const hardware=fs.readFileSync(path.join(ROOT,'app/src/main/assets/hardware-fidelity.js'),'utf8');
const html=fs.readFileSync(path.join(ROOT,'app/src/main/assets/index.html'),'utf8');
function fail(m){console.error('PRO ELECTRICAL SMOKE FAIL: '+m);process.exit(1)}function assert(c,m){if(!c)fail(m)}
for(const marker of ['window.__DSKY_PROCEED_ELECTRICAL__','const runtime = api?.runtimeTransitions','const input = api?.inputRuntime','typeof runtime.clockRequested','typeof runtime.onBeforeClock',"document.querySelector('[data-key=\"P\"]')","pro.addEventListener('pointerdown'","pro.addEventListener('pointerup'","pro.addEventListener('pointercancel'","document.addEventListener('visibilitychange'",'input.proceed(true)','input.proceed(false)','runtime.onBeforeClock(releaseProceed)',"window.AGCDSKY_SERVICE_REGISTRY.publish('AGCDSKY_PROCEED',Object.freeze({"])assert(source.includes(marker),`PRO controller missing lifecycle marker: ${marker}`);
for(const forbidden of ['api.proceedElectrical =','proceedPulse(','.proceedKey(','window.enterClock =','api.appStatus','api.getCore'])assert(!source.includes(forbidden),`PRO controller retained obsolete ownership: ${forbidden}`);
for(const forbidden of ["document.querySelector('[data-key=\"P\"]')",'proPointer','releaseProceed','proceedKey(true)','proceedKey(false)','hardwareEnterClock'])assert(!hardware.includes(forbidden),`hardware-fidelity.js retained PRO ownership: ${forbidden}`);
const runtimeIndex=html.indexOf('<script src="runtime-transitions.js"></script>'),inputIndex=html.indexOf('<script src="dsky-input-runtime.js"></script>'),hardwareIndex=html.indexOf('<script src="hardware-fidelity.js"></script>'),proceedIndex=html.indexOf('<script src="proceed-electrical.js"></script>');assert(runtimeIndex>=0&&inputIndex>runtimeIndex&&hardwareIndex>inputIndex&&proceedIndex>hardwareIndex,'PRO controller load order changed');
function makeEvent(pointerId){return{pointerId,prevented:false,immediate:false,preventDefault(){this.prevented=true},stopImmediatePropagation(){this.immediate=true}}}
const proListeners=Object.create(null),documentListeners=Object.create(null),classes=new Set(),calls=[],failures=[];let mode='agc',clockPending=false,throwOnPress=false,beforeClockHook=null,beforeClockRegistrations=0;
const pro={classList:{add:n=>classes.add(n),remove:n=>classes.delete(n)},addEventListener(type,fn){if(proListeners[type])fail(`duplicate PRO ${type} listener installed`);proListeners[type]=fn},setPointerCapture(pointerId){calls.push(['capture',pointerId])}};
const core={proceedKey(pressed){calls.push(['proceed',!!pressed]);if(pressed&&throwOnPress)throw new Error('synthetic PRO failure');return 1}};
const documentObject={hidden:false,querySelector(selector){return selector==='[data-key="P"]'?pro:null},addEventListener(type,fn){if(documentListeners[type])fail(`duplicate document ${type} listener installed`);documentListeners[type]=fn}};
const MODES=Object.freeze({CLOCK:'clock',AGC_LOADING:'agc-loading',AGC:'agc'});
const runtimeTransitions=Object.freeze({modes:MODES,mode(){return mode},core(){return core},clockRequested(){return clockPending},onBeforeClock(handler){beforeClockRegistrations++;beforeClockHook=handler;return()=>{if(beforeClockHook===handler)beforeClockHook=null}}});
const AGCDSKY={};
const context={console,document:documentObject,AGCDSKY,AGCDSKY_RUNTIME:runtimeTransitions,agcFailure(error){failures.push(String(error&&error.message||error))},__baseEnterClock(...args){calls.push(['enterClock',...args]);mode=MODES.CLOCK;return'clock-result'},window:null};context.window=context;const registry=installServiceRegistry(context);registry.publish('AGCDSKY_RUNTIME',runtimeTransitions,'PRO smoke runtime fixture');
Object.defineProperties(AGCDSKY,{runtimeTransitions:{enumerable:true,get:()=>context.AGCDSKY_RUNTIME||null},inputRuntime:{enumerable:true,get:()=>context.AGCDSKY_INPUT||null},proceedElectrical:{enumerable:true,get:()=>context.AGCDSKY_PROCEED||null}});
vm.createContext(context);vm.runInContext(inputSource,context,{filename:'dsky-input-runtime.js'});assert(context.AGCDSKY_INPUT===AGCDSKY.inputRuntime,'shared input getter did not resolve controller');vm.runInContext(source,context,{filename:'proceed-electrical.js'});
assert(AGCDSKY.proceedElectrical===context.AGCDSKY_PROCEED&&Object.isFrozen(AGCDSKY.proceedElectrical),'bootstrap PRO getter did not resolve frozen dedicated service');
assert(typeof proListeners.pointerdown==='function'&&typeof proListeners.pointerup==='function'&&typeof proListeners.pointercancel==='function','PRO pointer listeners not installed');assert(typeof documentListeners.visibilitychange==='function','visibility release listener not installed');assert(typeof beforeClockHook==='function'&&beforeClockRegistrations===1,'PRO did not register one pre-CLOCK cleanup hook');
vm.runInContext(source,context,{filename:'proceed-electrical-second-load.js'});assert(beforeClockRegistrations===1,'second PRO load duplicated CLOCK hook');
let event=makeEvent(41);proListeners.pointerdown(event);assert(event.prevented&&event.immediate&&classes.has('pressed'),'AGC PRO pointerdown not exclusively captured');assert(calls.filter(c=>c[0]==='proceed'&&c[1]===true).length===1,'PRO make count wrong');
event=makeEvent(42);proListeners.pointerdown(event);assert(calls.filter(c=>c[0]==='proceed'&&c[1]===true).length===1,'second pointer generated another make');proListeners.pointerup(makeEvent(42));assert(calls.filter(c=>c[0]==='proceed'&&c[1]===false).length===0,'non-owner released PRO');proListeners.pointerup(makeEvent(41));assert(calls.filter(c=>c[0]==='proceed'&&c[1]===false).length===1&&!classes.has('pressed'),'owner release failed');
event=makeEvent(51);proListeners.pointerdown(event);proListeners.pointercancel(makeEvent(51));assert(calls.filter(c=>c[0]==='proceed'&&c[1]===false).length===2,'pointercancel did not release PRO');event=makeEvent(61);proListeners.pointerdown(event);documentObject.hidden=true;documentListeners.visibilitychange();assert(calls.filter(c=>c[0]==='proceed'&&c[1]===false).length===3,'hidden transition did not release PRO');documentObject.hidden=false;
mode=MODES.AGC;event=makeEvent(71);proListeners.pointerdown(event);const beforeClock=calls.length;clockPending=true;beforeClockHook({reason:'app enterClock',from:MODES.AGC});const result=context.__baseEnterClock('test-clock',true);clockPending=false;const after=calls.slice(beforeClock);assert(result==='clock-result'&&after[0]?.[0]==='proceed'&&after[0]?.[1]===false&&after[1]?.[0]==='enterClock','pre-CLOCK hook did not release PRO before base transition');
mode=MODES.AGC;clockPending=true;const makesBefore=calls.filter(c=>c[0]==='proceed'&&c[1]===true).length;event=makeEvent(75);proListeners.pointerdown(event);assert(!event.prevented&&!event.immediate&&calls.filter(c=>c[0]==='proceed'&&c[1]===true).length===makesBefore,'PRO asserted after CLOCK request');clockPending=false;
mode=MODES.CLOCK;const beforeClockMode=calls.filter(c=>c[0]==='proceed'&&c[1]===true).length;event=makeEvent(81);proListeners.pointerdown(event);assert(!event.prevented&&!event.immediate&&calls.filter(c=>c[0]==='proceed'&&c[1]===true).length===beforeClockMode,'PRO asserted outside AGC mode');
mode=MODES.AGC;throwOnPress=true;event=makeEvent(91);proListeners.pointerdown(event);throwOnPress=false;assert(failures.length===1&&failures[0]==='synthetic PRO failure','failed PRO make did not reach agcFailure');assert(!classes.has('pressed')&&!AGCDSKY.proceedElectrical.state().held,'failed PRO make left ownership held');assert(calls.filter(c=>c[0]==='proceed'&&c[1]===false).length===5,'failed PRO make did not restore released level');
console.log('PRO electrical smoke: PASS');
console.log('  dedicated frozen service, registry-backed runtime fixture/input authority, bootstrap getter, maintained contact, lifecycle/CLOCK cleanup, suppression, idempotence, and failure cleanup verified');
