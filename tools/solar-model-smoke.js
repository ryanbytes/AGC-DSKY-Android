#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm');const ROOT=path.resolve(__dirname,'..'),ASSETS=path.join(ROOT,'app/src/main/assets'),source=fs.readFileSync(path.join(ASSETS,'display-environment.js'),'utf8'),shell=fs.readFileSync(path.join(ASSETS,'app-shell-runtime.js'),'utf8'),api=fs.readFileSync(path.join(ASSETS,'agc-api-runtime.js'),'utf8');function assert(c,m){if(!c)throw new Error(m)}function close(a,e,t,m){assert(Math.abs(a-e)<=t,`${m}: got ${a}, expected ${e} ± ${t}`)}
const start=source.indexOf('const DAY_MS=86400000'),end=source.indexOf('function requestSolarLocation()');assert(start>=0&&end>start,'could not isolate production solar-model functions');for(const token of ['const DAY_MS=86400000','function solarTimes('])assert(!shell.includes(token)&&!api.includes(token),`non-environment runtime regained solar ownership: ${token}`);assert(source.includes('const environmentShell=window.AGCDSKY_SHELL;')&&source.includes('environmentShell.accurateDate()'),'solar model does not consume shell time service');
const solarSource=source.slice(start,end)+'\nglobalThis.__solarModel={solarTimes,currentSolarFactor,hourAngle,smoothstep};';function fixedDateClass(nowIso){const RealDate=Date,fixedMs=RealDate.parse(nowIso);assert(Number.isFinite(fixedMs),`invalid fixed date: ${nowIso}`);return class FixedDate extends RealDate{constructor(...a){super(...(a.length?a:[fixedMs]))}static now(){return fixedMs}}}
function loadModel(nowIso,lat=null,lon=null){const storage=new Map();if(lat!==null)storage.set('solarLat',String(lat));if(lon!==null)storage.set('solarLon',String(lon));const FixedDate=fixedDateClass(nowIso),store={get:k=>storage.has(k)?storage.get(k):null};const context={Math,Number,parseFloat,Date:FixedDate,environmentShell:{store,accurateDate(){return new FixedDate()}}};context.globalThis=context;vm.createContext(context);vm.runInContext(solarSource,context,{filename:'display-environment.js:solar-model'});return context.__solarModel}
function daylightHours(times){return(times.sunset.getTime()-times.sunrise.getTime())/3600000}
const equinox=loadModel('2026-03-20T12:00:00Z'),equinoxTimes=equinox.solarTimes(new Date('2026-03-20T12:00:00Z'),0,0);assert(equinoxTimes&&equinoxTimes.sunrise instanceof Date&&equinoxTimes.sunset instanceof Date,'equatorial equinox must produce sunrise and sunset');const equinoxDaylight=daylightHours(equinoxTimes);assert(equinoxDaylight>12.0&&equinoxDaylight<12.3,`equatorial daylight implausible: ${equinoxDaylight} h`);const polarSummer=loadModel('2026-06-21T12:00:00Z',80,0);assert(polarSummer.solarTimes(new Date('2026-06-21T12:00:00Z'),80,0)===null&&polarSummer.currentSolarFactor()===0,'polar-summer fallback changed');const polarWinter=loadModel('2026-12-21T12:00:00Z',80,0);assert(polarWinter.solarTimes(new Date('2026-12-21T12:00:00Z'),80,0)===null&&polarWinter.currentSolarFactor()===0,'polar-winter fallback changed');const sampleLat=35,sampleLon=-100,sample=loadModel('2026-08-22T16:00:00Z'),sampleTimes=sample.solarTimes(new Date('2026-08-22T16:00:00Z'),sampleLat,sampleLon);assert(sampleTimes&&sampleTimes.sunrise.getTime()<sampleTimes.sunset.getTime(),'mid-latitude sunrise must precede sunset');const sampleDaylight=daylightHours(sampleTimes);assert(sampleDaylight>12.5&&sampleDaylight<14.5,`mid-latitude August daylight implausible: ${sampleDaylight} h`);const atSunrise=loadModel(sampleTimes.sunrise.toISOString(),sampleLat,sampleLon);close(atSunrise.currentSolarFactor(),0.5,0.03,'DREAM SOLAR sunrise transition midpoint');
console.log('solar model smoke: PASS');console.log(`  equator equinox daylight: ${equinoxDaylight.toFixed(3)} h`);console.log('  high-latitude no-crossing fallback: dim, no fabricated event');console.log(`  mid-latitude Aug 22 daylight: ${sampleDaylight.toFixed(3)} h`);
