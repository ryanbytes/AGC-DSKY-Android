#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..'),ASSETS=path.join(ROOT,'app/src/main/assets'),display=fs.readFileSync(path.join(ASSETS,'agc-display-runtime.js'),'utf8'),shell=fs.readFileSync(path.join(ASSETS,'app-shell-runtime.js'),'utf8'),api=fs.readFileSync(path.join(ASSETS,'agc-api-runtime.js'),'utf8'),hw=fs.readFileSync(path.join(ASSETS,'hardware-fidelity.js'),'utf8'),index=fs.readFileSync(path.join(ASSETS,'index.html'),'utf8');function assert(c,m){if(!c)throw new Error(m)}
assert(display.includes("function updateAgcCompActy(){displayRenderer.setLamp('comp',!!(agcCh11Value&0o00002))}"),'COMP ACTY must follow channel 011 bit 2 through renderer service');assert(display.includes('function baseDecodeChannel11(value){agcCh11Value=value;updateAgcCompActy();'),'channel 011 decoder must immediately update COMP ACTY');assert(display.includes("compat.mutable('decodeChannel11',baseDecodeChannel11"),'channel 011 compatibility slot must remain display-owned');assert(display.includes('const displayRenderer=window.AGCDSKY_RENDERER;'),'AGC display does not use renderer service');assert(!shell.includes('function baseDecodeChannel11(value)')&&!api.includes('function baseDecodeChannel11(value)'),'non-display runtime regained channel decoder ownership');assert(!fs.existsSync(path.join(ASSETS,'app.js')),'legacy app.js unexpectedly exists');assert(!index.includes('relay-output-sync.js'),'relay-output-sync presentation shim must not be loaded');assert(!hw.includes('audioPresentationLatencyMs'),'hardware fidelity must not delay EL presentation for audio latency');assert(!hw.includes('AUDIO_PRESENTATION_'),'audio presentation timing constants must remain removed');const driveStart=hw.indexOf('function beginRelayDrive'),driveEnd=hw.indexOf('\n\n\n  // Real yaAGC output',driveStart);assert(driveStart>=0&&driveEnd>driveStart,'could not isolate beginRelayDrive');const drive=hw.slice(driveStart,driveEnd);assert(drive.includes('if (render) applyRelayLow11(relay, low11);'),'settled relay state must paint in 20-ms commit callback');assert(drive.includes('}, RELAY_DRIVE_MS);'),'relay latch/display commit must remain on 20-ms physical boundary');console.log('DSKY output path smoke: PASS');console.log('  AGC display owns the channel-011 slot; late fidelity remains behind the audited compatibility boundary; EL rows retain the 20-ms settled boundary');
