#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm');
const {installServiceRegistry}=require('./test-service-registry');
const ROOT=path.resolve(__dirname,'..'),keycodeSource=fs.readFileSync(path.join(ROOT,'app/src/main/assets/dsky-keycodes.js'),'utf8'),inputSource=fs.readFileSync(path.join(ROOT,'app/src/main/assets/dsky-input-runtime.js'),'utf8'),keyboardSource=fs.readFileSync(path.join(ROOT,'app/src/main/assets/keyboard-electrical-interlock.js'),'utf8');
function fail(m){console.error('KEYBOARD CLOCK CANCEL SMOKE FAIL: '+m);process.exit(1)}function assert(c,m){if(!c)fail(m)}
for(const marker of ['if (state.cancelled || !clockHandoffPending || runtime.clockRequested()) return','state.cancelled = true','if (runtime.clockRequested()) return','function releaseForClock()','runtime.onBeforeClock(releaseForClock)',"window.AGCDSKY_SERVICE_REGISTRY.publish('AGCDSKY_KEYBOARD_ELECTRICAL',Object.freeze({"])assert(keyboardSource.includes(marker),`keyboard cancellation marker missing: ${marker}`);assert(!keyboardSource.includes('api.keyboardElectrical ='),'keyboard regained late facade mutation');
const MODES=Object.freeze({CLOCK:'clock',AGC_LOADING:'agc-loading',AGC:'agc'}),windowListeners=Object.create(null),documentListeners=Object.create(null),calls=[],timers=new Map();let nextTimer=1,nowMs=0,mode=MODES.CLOCK,clockPending=false,releaseLoad=null,beforeClockHook=null;
function addListener(bucket,type,fn){(bucket[type]||=[]).push(fn)}function dispatch(bucket,type,event){for(const fn of bucket[type]||[]){fn(event);if(event.immediate)break}}function makeButton(key){const classes=new Set();return{dataset:{key},classList:{add:n=>classes.add(n),remove:n=>classes.delete(n),contains:n=>classes.has(n)},setPointerCapture(){},releasePointerCapture(){}}}function makeEvent(button,pointerId){return{pointerId,target:{closest:s=>s==='[data-key]'?button:null},prevented:false,stopped:false,immediate:false,preventDefault(){this.prevented=true},stopPropagation(){this.stopped=true},stopImmediatePropagation(){this.immediate=true}}}async function flushMicrotasks(count=12){for(let i=0;i<count;i++)await Promise.resolve()}
const core={keyPress(code){calls.push(['make',code,nowMs]);return 1},keyRelease(){calls.push(['reset',nowMs]);return true}};
const runtimeTransitions=Object.freeze({modes:MODES,mode(){return mode},core(){return core},clockRequested(){return clockPending},requestAgc(reason){calls.push(['request-agc',reason,nowMs]);if(clockPending)return Promise.reject(new Error('CLOCK pending'));if(mode===MODES.AGC)return Promise.resolve({mode});mode=MODES.AGC_LOADING;return new Promise(resolve=>{releaseLoad=()=>{mode=MODES.AGC;calls.push(['agc-ready',nowMs]);resolve({mode})}})},onBeforeClock(handler){beforeClockHook=handler;return()=>{if(beforeClockHook===handler)beforeClockHook=null}}});
const AGCDSKY={scheduleAgcAutosave(reason){calls.push(['autosave',reason,nowMs])},hardwarePersonality(){return{keys:{V:{contactMs:10,returnSoundMs:5}}}}};
const context={AGCDSKY,AGCDSKY_RUNTIME:runtimeTransitions,console,Promise,performance:{now(){return nowMs}},localStorage:{getItem(){return'0'}},setTimeout(fn,delay=0){const id=nextTimer++;timers.set(id,{fn,due:nowMs+Math.max(0,Number(delay)||0)});return id},clearTimeout(id){timers.delete(id)},window:null,document:{hidden:false,addEventListener(type,fn){addListener(documentListeners,type,fn)}},press(key){calls.push(['legacy-clock-press',key,nowMs])}};context.window=context;context.addEventListener=(type,fn)=>addListener(windowListeners,type,fn);installServiceRegistry(context);
Object.defineProperties(AGCDSKY,{runtimeTransitions:{enumerable:true,get:()=>context.AGCDSKY_RUNTIME||null},inputRuntime:{enumerable:true,get:()=>context.AGCDSKY_INPUT||null},keyboardElectrical:{enumerable:true,get:()=>context.AGCDSKY_KEYBOARD_ELECTRICAL||null}});
vm.createContext(context);vm.runInContext(keycodeSource,context,{filename:'dsky-keycodes.js'});vm.runInContext(inputSource,context,{filename:'dsky-input-runtime.js'});assert(context.AGCDSKY_INPUT===AGCDSKY.inputRuntime,'bootstrap input getter did not resolve controller');vm.runInContext(keyboardSource,context,{filename:'keyboard-electrical-interlock.js'});assert(AGCDSKY.keyboardElectrical===context.AGCDSKY_KEYBOARD_ELECTRICAL&&Object.isFrozen(AGCDSKY.keyboardElectrical),'bootstrap keyboard getter did not resolve frozen service');assert(typeof beforeClockHook==='function','keyboard did not register pre-CLOCK cancellation hook');
(async()=>{const verb=makeButton('V');let event=makeEvent(verb,41);dispatch(windowListeners,'pointerdown',event);event=makeEvent(verb,41);dispatch(windowListeners,'pointerup',event);let state=AGCDSKY.keyboardElectrical.state();assert(mode===MODES.AGC_LOADING&&state.clockHandoffPending&&state.cycleLatched,'fixture did not enter pending CLOCK -> AGC handoff');assert(calls.filter(c=>c[0]==='make').length===0,'key emitted before AGC ready');clockPending=true;beforeClockHook({reason:'app enterClock',from:MODES.AGC_LOADING});state=AGCDSKY.keyboardElectrical.state();assert(!state.clockHandoffPending&&!state.cycleLatched&&!state.electricalMade,'pre-CLOCK cleanup did not cancel handoff');assert(!verb.classList.contains('pressed'),'cleanup left key pressed');const later=makeButton('V');event=makeEvent(later,51);dispatch(windowListeners,'pointerdown',event);assert(event.prevented&&event.immediate,'new key during pending CLOCK was not swallowed');assert(AGCDSKY.keyboardElectrical.state().down===0,'pending CLOCK created pointer ownership');releaseLoad();await flushMicrotasks();assert(calls.filter(c=>c[0]==='make').length===0,'canceled handoff resurrected ghost make');assert(!calls.some(c=>c[0]==='autosave'),'canceled handoff scheduled autosave');state=AGCDSKY.keyboardElectrical.state();assert(!state.clockHandoffPending&&!state.cycleLatched&&!state.electricalMade,'canceled handoff re-latched state');console.log('keyboard CLOCK cancel smoke: PASS');console.log('  explicit keyboard service publication preserves pending first-key cancellation, suppresses later contacts, and prevents ghost makes after AGC loading');})().catch(e=>fail(e&&e.stack?e.stack:String(e)));
