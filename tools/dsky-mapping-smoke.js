#!/usr/bin/env node
'use strict';

const fs=require('fs'),path=require('path'),vm=require('vm');
const ROOT=path.resolve(__dirname,'..'),ASSETS=path.join(ROOT,'app/src/main/assets');
const shell=fs.readFileSync(path.join(ASSETS,'app-shell-runtime.js'),'utf8'),api=fs.readFileSync(path.join(ASSETS,'agc-api-runtime.js'),'utf8'),clock=fs.readFileSync(path.join(ASSETS,'phone-clock-runtime.js'),'utf8'),display=fs.readFileSync(path.join(ASSETS,'agc-display-runtime.js'),'utf8'),geometry=fs.readFileSync(path.join(ASSETS,'dsky-geometry.js'),'utf8'),keycodesSource=fs.readFileSync(path.join(ASSETS,'dsky-keycodes.js'),'utf8'),relayAudio=fs.readFileSync(path.join(ASSETS,'relay-audio-refine.js'),'utf8'),relayMatrix=fs.readFileSync(path.join(ASSETS,'dsky-relay-matrix.js'),'utf8'),indexHtml=fs.readFileSync(path.join(ASSETS,'index.html'),'utf8');
function assert(c,m){if(!c)throw new Error(m)}function literal(source,name){const pattern=new RegExp('const\\s+'+name+'\\s*=\\s*(?:Object\\.freeze\\()?'+ '(\\{[\\s\\S]*?\\}|\\[[\\s\\S]*?\\])\\)?;');const match=source.match(pattern);assert(match,`could not locate ${name}`);return vm.runInNewContext('('+match[1]+')')}function same(a,e,m){assert(JSON.stringify(a)===JSON.stringify(e),`${m}\n  actual: ${JSON.stringify(a)}\n  expected: ${JSON.stringify(e)}`)}
const expectedDigitRelay={' ':0,'0':21,'1':3,'2':25,'3':27,'4':15,'5':30,'6':28,'7':19,'8':29,'9':31};
const digitRelay=literal(clock,'DIGIT_RELAY_VALUE');same(digitRelay,expectedDigitRelay,'DSKY five-relay digit table changed');const relayDigit=literal(display,'RELAY_DIGIT');for(const [digit,code] of Object.entries(expectedDigitRelay))assert(relayDigit[code]===digit,`incoming relay decoder no longer maps code ${code} back to ${JSON.stringify(digit)}`);
assert(clock.includes("compat.readonly('DIGIT_RELAY',()=>DIGIT_RELAY_VALUE)"),'clock relay table is not exposed through compatibility boundary');assert(display.includes("compat.readonly('RELAY_DIGIT',()=>RELAY_DIGIT)"),'base incoming relay table is not exposed read-only to the schematic layer');assert(display.includes("relayDigitSlot=compat.mutable('relayDigit',relayDigitImpl,isFn)"),'schematic relay decoder must be a mutable display-owned slot');
const expectedKeys={'1':0o01,'2':0o02,'3':0o03,'4':0o04,'5':0o05,'6':0o06,'7':0o07,'8':0o10,'9':0o11,'0':0o20,V:0o21,R:0o22,K:0o31,'+':0o32,'-':0o33,E:0o34,C:0o36,N:0o37};const keyContext={window:null,Object};keyContext.window=keyContext;vm.createContext(keyContext);vm.runInContext(keycodesSource,keyContext,{filename:'dsky-keycodes.js'});same(JSON.parse(JSON.stringify(keyContext.AGCDSKY_KEY_CODES)),expectedKeys,'Pinball key-code table changed');
const groups=literal(clock,'CLOCK_GROUPS_VALUE');same(groups,[{relay:8,cells:[['r1',0]],singleRight:true},{relay:7,cells:[['r1',1],['r1',2]],b:1},{relay:6,cells:[['r1',3],['r1',4]],b:0},{relay:5,cells:[['r2',0],['r2',1]],b:1},{relay:4,cells:[['r2',2],['r2',3]],b:0},{relay:3,cells:[['r2',4],['r3',0]],b:0},{relay:2,cells:[['r3',1],['r3',2]],b:1},{relay:1,cells:[['r3',3],['r3',4]],b:0}],'phone-clock Block II relay grouping changed');
const clockStart=clock.indexOf('function clockWordImpl(group,want)'),clockEnd=clock.indexOf('function popcount11Impl',clockStart);assert(clockStart>=0&&clockEnd>clockStart,'could not isolate clockWordImpl');const clockContext={DIGIT_RELAY_VALUE:expectedDigitRelay};vm.createContext(clockContext);vm.runInContext(clock.slice(clockStart,clockEnd)+'\nthis.clockWordImpl=clockWordImpl;',clockContext);const want={r1:['1','2','3','4','5'],r2:['6','7','8','9','0'],r3:['2','4','6','8','0']};for(const group of groups){let c=0,d=0;if(group.singleRight)d=expectedDigitRelay[want[group.cells[0][0]][group.cells[0][1]]];else{c=expectedDigitRelay[want[group.cells[0][0]][group.cells[0][1]]];d=expectedDigitRelay[want[group.cells[1][0]][group.cells[1][1]]]}const expected=((group.b||0)<<10)|(c<<5)|d;assert(clockContext.clockWordImpl(group,want)===expected,`clock relay ${group.relay} encoding changed`)}
const cmLampOrder=['uplink','temp','noatt','gimbal','stby','prog','keyrel','restart','oprerr','tracker'];let priorLampPos=-1;for(const lamp of cmLampOrder){const pos=indexHtml.indexOf(`data-lamp="${lamp}"`);assert(pos>priorLampPos,`CM annunciator ${lamp} is missing or out of physical row order`);priorLampPos=pos}const blankLampCount=(indexHtml.match(/class="lamp (?:white|yellow) blank"/g)||[]).length;assert(blankLampCount===4,`CM annunciator bank must keep exactly four blank positions; found ${blankLampCount}`);assert(!indexHtml.includes('data-lamp="alt"'),'CM DSKY face must not expose LM ALT annunciator');assert(!indexHtml.includes('data-lamp="vel"'),'CM DSKY face must not expose LM VEL annunciator');
assert(display.includes('const relay=(value>>11)&0o17,b=(value>>10)&1,c=(value>>5)&0o37,d=value&0o37,low11=value&0o3777;'),'channel 010 must retain 4-bit bank selector + low-11 relay split');assert(display.includes('if(relay>=1&&relay<=12)'),'channel 010 must retain all 12 relay banks');assert(display.includes('displayPopcount11(prior^low11)'),'AGC relay sounds must derive from changed low-11 bistable relays');assert(clock.includes('CLOCK_SETTLE_MS=20'),'Block II relay settle interval must remain 20 ms');assert(!fs.existsSync(path.join(ASSETS,'app.js')),'legacy app.js unexpectedly exists');for(const [name,source] of [['app-shell-runtime.js',shell],['agc-api-runtime.js',api]])assert(!source.includes('function baseDecodeChannel10(')&&!source.includes('const RELAY_DIGIT='),`${name} regained AGC display/mapping ownership`);assert(display.includes('const displayState=window.AGCDSKY_APP_STATE;'),'AGC display runtime is not bound to shared app state');assert(geometry.includes('const geometryState=window.AGCDSKY_APP_STATE;'),'DSKY geometry is not bound to shared app state');assert(relayAudio.includes('count = Math.max(0, Math.min(11'),'relay audio must cap bank transition to 11 relays');assert(relayAudio.includes('for (let i = 0; i < count; i++) audio.emitTick'),'relay audio must emit one current-service transient per changed relay');assert(relayAudio.includes('mechanically scattered relay'),'relay audio must document parallel electrical drive vs mechanical scatter');assert(relayAudio.includes("compat.replace('emitTick',recreatedDskyRelayClick")&&relayAudio.includes("compat.replace('playRelayBurst',recreatedRelayBankClicks"),'relay audio refinement must register explicit service-slot replacements');
const matrixStart=relayMatrix.indexOf('function segmentsForRelayCode(value)'),matrixEnd=relayMatrix.indexOf('const physicalChars',matrixStart);assert(matrixStart>=0&&matrixEnd>matrixStart,'could not isolate schematic relay-contact matrix');const matrixContext={};vm.createContext(matrixContext);vm.runInContext(relayMatrix.slice(matrixStart,matrixEnd)+'\nthis.segmentsForRelayCode=segmentsForRelayCode;',matrixContext);const expectedSegments={' ':'','0':'abcdef','1':'bc','2':'abdeg','3':'abcdg','4':'bcfg','5':'acdfg','6':'acdefg','7':'abc','8':'abcdefg','9':'abcdfg'};for(const [digit,code] of Object.entries(expectedDigitRelay))assert(matrixContext.segmentsForRelayCode(code)===expectedSegments[digit],`schematic relay contacts no longer render ${JSON.stringify(digit)} correctly`);const allContactPatterns=new Set();for(let code=0;code<32;code++)allContactPatterns.add(matrixContext.segmentsForRelayCode(code));assert(allContactPatterns.size>11,'relay contact renderer collapsed 32 physical K1-K5 states to decimal enum');assert(relayMatrix.includes('segments[ch] = segmentsForRelayCode(code);'),'non-decimal K1-K5 states must be wired into renderer segment table');assert(relayMatrix.includes("compat.replace('relayDigit',schematicRelayDigit,'schematic K1-K5 relay matrix')"),'hardware relay decoder must explicitly replace the display-owned relayDigit slot');assert(!relayMatrix.includes('relayDigit = function'),'schematic decoder must not mutate relayDigit directly');const geometryPos=indexHtml.indexOf('<script src="dsky-geometry.js"></script>'),matrixPos=indexHtml.indexOf('<script src="dsky-relay-matrix.js"></script>'),hardwarePos=indexHtml.indexOf('<script src="hardware-fidelity.js"></script>');assert(geometryPos>=0&&matrixPos>geometryPos&&hardwarePos>matrixPos,'schematic matrix must load after geometry and before fidelity timing');
const decodeStart=display.indexOf('function baseDecodeChannel10(value)'),decodeEnd=display.indexOf('function updateAgcCompActy()',decodeStart);assert(decodeStart>=0&&decodeEnd>decodeStart,'could not isolate current channel 010 decoder');const decode=display.slice(decodeStart,decodeEnd);for(const snippet of ["case 12:displayRenderer.setLamp('vel',value&0o00004);displayRenderer.setLamp('noatt',value&0o00010);displayRenderer.setLamp('alt',value&0o00020);displayRenderer.setLamp('gimbal',value&0o00040);displayRenderer.setLamp('tracker',value&0o00200);displayRenderer.setLamp('prog',value&0o00400)","case 11:agcDisplayValue.prog[0]=relayDigitSlot.get()(c);agcDisplayValue.prog[1]=relayDigitSlot.get()(d)","case 10:agcDisplayValue.verb[0]=relayDigitSlot.get()(c);agcDisplayValue.verb[1]=relayDigitSlot.get()(d)","case 9:agcDisplayValue.noun[0]=relayDigitSlot.get()(c);agcDisplayValue.noun[1]=relayDigitSlot.get()(d)","case 8:agcDisplayValue.r1.digits[0]=relayDigitSlot.get()(d)","case 7:agcDisplayValue.r1.plus=!!b","case 6:agcDisplayValue.r1.minus=!!b","case 5:agcDisplayValue.r2.plus=!!b","case 4:agcDisplayValue.r2.minus=!!b","case 3:agcDisplayValue.r2.digits[4]=relayDigitSlot.get()(c);agcDisplayValue.r3.digits[0]=relayDigitSlot.get()(d)","case 2:agcDisplayValue.r3.plus=!!b","case 1:agcDisplayValue.r3.minus=!!b"])assert(decode.includes(snippet),`channel 010 mapping missing: ${snippet}`);
assert(display.includes("displayRenderer.setLamp('comp',!!(agcCh11Value&0o00002))"),'channel 011 COMP ACTY mapping changed');assert(display.includes("displayRenderer.setLamp('uplink',value&0o00004)"),'channel 011 UPLINK ACTY mapping changed');assert(display.includes('agcCh13Value=value;'),'channel 013 state must remain latched for diagnostics');for(const snippet of ["displayRenderer.setLamp('temp',value&0o00010)","displayRenderer.setLamp('keyrel',value&0o00020)","classList.toggle('vn-flash-off',!!(value&0o00040))","displayRenderer.setLamp('oprerr',value&0o00100)","displayRenderer.setLamp('restart',value&0o00200)","displayRenderer.setLamp('stby',value&0o00400)","classList.toggle('el-off',!!(value&0o01000))"])assert(display.includes(snippet),`channel 0163 mapping missing: ${snippet}`);
console.log('DSKY mapping smoke: PASS');console.log('  service-owned mappings, explicit K1-K5 decoder replacement, explicit relay-audio refinements, CM annunciators, and channel mappings verified');
